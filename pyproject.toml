[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-in-action"
version = "0"
requires-python = ">=3.12"

[tool.setuptools]
py-modules = ["env"]
package-dir = {"" = "part2-agent-frameworks"}
