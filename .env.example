# 复制成 .env 填上自己的 key：cp .env.example .env
# 真 key 只写进 .env，它在 .gitignore 里；这个模板是要提交的，别往里填。
#
# 终端 export 过的、IDE 运行配置里填的，都压过 .env。

OPENAI_API_KEY=sk-...

# 下面两个有默认值，不改也能跑
OPENAI_BASE_URL=https://openrouter.ai/api/v1
MODEL=qwen/qwen-2.5-72b-instruct
